# 壹点灵爬虫配置示例
# 复制此文件为 .env 并根据需要修改

# 目标站点
TARGET_ROOT=https://m.ydl.com/ask
BASE_URL=https://m.ydl.com

# 时间范围（可选，格式：YYYY-MM-DD）
# START_RANGE=2025-01-01

# 移动端 User Agent
UA_MOBILE=Mozilla/5.0 (iPhone; CPU iPhone OS 14_7_1 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/14.1.2 Mobile/15E148 Safari/604.1

# 限速配置
QPS_LIMIT=0.5          # 每秒查询数（0.5 = 每2秒1个请求）
BURST=2                # 最大突发并发数
RETRY=3                # 失败重试次数
BACKOFF_BASE=1.0       # 基础退避时间（秒）
BACKOFF_MAX=60.0       # 最大退避时间（秒）
JITTER_MIN=0.1         # 最小抖动比例（10%）
JITTER_MAX=0.3         # 最大抖动比例（30%）

# 滚动配置
SCROLL_STEP=1500       # 每次滚动像素
SCROLL_MAX_EMPTY=8     # 最大空滚动次数

# 存储配置
DATA_DIR=data
LOGS_DIR=logs
CHECKPOINT_FILE=data/checkpoint.json

# 告警配置（可选）
# ALERT_SINK=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Playwright 配置
HEADLESS=true          # 是否使用无头模式
VIEWPORT_WIDTH=375     # 视窗宽度（移动端）
VIEWPORT_HEIGHT=812    # 视窗高度（移动端）

# 对象存储（可选，用于云端备份）
# OBJECT_STORE=s3://your-bucket/path

